{
  "name": "bridge-monitor",
  "version": "0.1.0",
  "private": true,
  "description": "Scans a list of application form URLs with the BRIDGE scanner and writes one barrier report per URL per run",
  "type": "module",
  "scripts": {
    "scan": "node run.mjs urls.txt",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "0.25.12",
    "playwright": "1.56.0",
    "typescript": "5.9.3"
  }
}
