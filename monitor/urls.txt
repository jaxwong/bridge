# URLs the monitor scans, one per line. Blank lines and # comments are ignored.
#
# Acme Careers, the local fixture. Serve it first, from the repo root:
#   python3 -m http.server 8765 -d extension/test/fixtures/acme
#
# The three versions stand in for one form changing over time, which is why the dashboard
# groups them as one form: pagePath excludes the query string (bridge-user.md §6.7).
#
# Scan them on SEPARATE RUNS, not all in one pass. The demo is v1 today, v2 after the
# "fix", v3 after the "regression"; three scans of one form written seconds apart would
# make the comparison meaningless. Leave exactly one of the three uncommented per run.
http://localhost:8765/
# http://localhost:8765/?v=2
# http://localhost:8765/?v=3

# At least one real public posting, for credibility (bridge-business.md §6.5).
#
# NOT FILLED IN. A live Greenhouse posting URL goes here. It has to be verified to load on
# the morning of the demo — postings expire, which bridge-business.md §9 lists as a known
# risk, and the monitor exits non-zero if one stops resolving rather than writing an empty
# report that would read as "this form is fine".
