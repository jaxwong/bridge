{
  "name": "bridge-dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "BRIDGE employer dashboard: loads barrier reports and shows what changed between scans",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --experimental-strip-types test/compare.test.ts",
    "test:a11y": "npm run build && node test/a11y.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.20.4",
    "axe-core": "^4.13.0",
    "playwright": "^1.56.0",
    "typescript": "5.9.3",
    "vite": "^7.3.6"
  }
}
